<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= pageTitle %></title>
    <link rel="icon" type="image/x-icon" href="" />
    <!-- Style Library & Framework -->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"
    />

    <!-- Style File -->
    <link rel="stylesheet" href="/setup.css" />
    <link rel="stylesheet" href="/admin/styles/style.css" />
  </head>
  <body>
    <%-include("../../partials/header.view.ejs") %>

    <div class="body">
      <%-include("../../partials/sider.view.ejs") %>
      <div class="main">
        <%-include("../../partials/alertMessage.view.ejs") %>
        <div class="row">
          <div class="col-xxl-4 col-xl-4 col-lg-4 col-md-4 col-sm-12 col-12">
            <h1><%= pageTitle %></h1>
          </div>
          <div class="col-xxl-4 col-xl-4 col-lg-4 col-md-4 col-sm-12 col-12"></div>
          <div class="col-xxl-4 col-xl-4 col-lg-4 col-md-4 col-sm-12 col-12">
            <%#-include("../../partials/search.view.ejs") %>
          </div>
        </div>
        <div class="card mb-3">
          <div class="card-header">
            <div class="row">
              <div class="col-6">Danh sách</div>
              <div class="col-6">
                <button type="submit" class="btn btn-outline-primary float-end" id="button-submit">
                  <i class="bi bi-pencil-square me-1"></i>Cập nhật
                </button>
              </div>
            </div>
          </div>
          <div class="card-body">
            <% if (roleList.length === 0) {%>
            <div class="text-center text-white bg-danger p-2">
              Không tìm thấy quyền trong danh sách
            </div>
            <% } %>
            <div data-role="<%= roleListJon %>"></div>
            <table class="table table-hover table-sm" id="table-permission">
              <thead>
                <th class="bg-primary-subtle text-primary text-decoration-underline">
                  Các tính năng
                </th>
                <% roleList.forEach(role => { %>
                <th class="bg-primary-subtle text-center text-primary text-decoration-underline">
                  <%= role.title %>
                </th>
                <% }) %>
              </thead>
              <tbody>
                <tr data-name="id" class="d-none">
                  <td class="bg-body-secondary"><b>ID nhóm quyền</b></td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center bg-body-secondary">
                    <input type="text" value="<%= role._id %>" />
                  </td>
                  <% }) %>
                </tr>

                <!-- Các quyền dành cho sản phẩm -->
                <tr>
                  <td class="bg-body-secondary"><b>Các quyền về sản phẩm</b></td>
                  <% roleList.forEach(role => { %>
                  <td class="bg-body-secondary"></td>
                  <% }) %>
                </tr>
                <tr data-name="product-view">
                  <td>Xem</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="product-add">
                  <td>Thêm mới</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="product-change-status">
                  <td>Đổi trạng thái</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="product-update">
                  <td>Chỉnh sửa</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="product-sort-delete">
                  <td>Xóa mềm</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="product-restore">
                  <td>Khôi phục</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="product-hard-delete">
                  <td>Xóa cứng</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>

                <!-- Các quyền dành cho danh mục -->
                <tr>
                  <td class="bg-body-secondary"><b>Các quyền về danh mục sản phẩm</b></td>
                  <% roleList.forEach(role => { %>
                  <td class="bg-body-secondary"></td>
                  <% }) %>
                </tr>
                <tr data-name="category-view">
                  <td>Xem</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="category-add">
                  <td>Thêm mới</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="category-change-status">
                  <td>Đổi trạng thái</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="category-update">
                  <td>Chỉnh sửa</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="category-sort-delete">
                  <td>Xóa mềm</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="category-restore">
                  <td>Khôi phục</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="category-hard-delete">
                  <td>Xóa cứng</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>

                <!-- Các quyền dành cho tài khoản -->
                <tr>
                  <td class="bg-body-secondary"><b>Các quyền về tài khoản</b></td>
                  <% roleList.forEach(role => { %>
                  <td class="bg-body-secondary"></td>
                  <% }) %>
                </tr>
                <tr data-name="account-view">
                  <td>Xem</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="account-add">
                  <td>Thêm mới</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="account-change-status">
                  <td>Đổi trạng thái</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="account-update">
                  <td>Chỉnh sửa</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="account-sort-delete">
                  <td>Xóa mềm</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="account-restore">
                  <td>Khôi phục</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="account-hard-delete">
                  <td>Xóa cứng</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>

                <!-- Các quyền dành cho đơn hàng -->
                <tr>
                  <td class="bg-body-secondary"><b>Các quyền về đơn hàng</b></td>
                  <% roleList.forEach(role => { %>
                  <td class="bg-body-secondary"></td>
                  <% }) %>
                </tr>
                <tr data-name="order-view">
                  <td>Xem</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="order-change-status">
                  <td>Đổi trạng thái</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="order-update">
                  <td>Chỉnh sửa</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="order-sort-delete">
                  <td>Xóa mềm</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="order-restore">
                  <td>Khôi phục</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="order-hard-delete">
                  <td>Xóa cứng</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>

                <!-- Các quyền dành cho nhóm quyền -->
                <tr>
                  <td class="bg-body-secondary"><b>Các quyền về nhóm quyền</b></td>
                  <% roleList.forEach(role => { %>
                  <td class="bg-body-secondary"></td>
                  <% }) %>
                </tr>
                <tr data-name="role-view">
                  <td>Xem</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="role-add">
                  <td>Thêm mới</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="role-update">
                  <td>Chỉnh sửa</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="role-sort-delete">
                  <td>Xóa mềm</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="role-restore">
                  <td>Khôi phục</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="role-hard-delete">
                  <td>Xóa cứng</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>

                <!-- Các quyền dành cho phân quyền -->
                <tr>
                  <td class="bg-body-secondary"><b>Các quyền về phân quyền</b></td>
                  <% roleList.forEach(role => { %>
                  <td class="bg-body-secondary"></td>
                  <% }) %>
                </tr>
                <tr data-name="permission-view">
                  <td>Xem</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
                <tr data-name="permission-update">
                  <td>Chỉnh sửa</td>
                  <% roleList.forEach(role => { %>
                  <td class="text-center"><input type="checkbox" /></td>
                  <% }) %>
                </tr>
              </tbody>
            </table>
          </div>

          <form
            method="POST"
            action="<%= prefixAdmin %>/permissions?_method=PATCH"
            class="d-none"
            id="form-change-permission"
          >
            <div class="form-group">
              <input type="text" name="permission" class="form-control" />
            </div>
          </form>
        </div>
      </div>
    </div>

    <!-- Script File -->
    <script src="/admin/scripts/permission/update.js"></script>
    <script src="/admin/scripts/showAlert.js"></script>
    <script src="/admin/scripts/toggleAccountInfo.js"></script>

    <!-- Script Library & Framwork -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://kit.fontawesome.com/aaf479b433.js" crossorigin="anonymous"></script>
    <script src="../reload/reload.js"></script>
  </body>
</html>
